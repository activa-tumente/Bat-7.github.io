import LoginForm from '../../components/auth/LoginForm';

/**
 * Página de login que utiliza el componente LoginForm mejorado
 * que soporta autenticación con email o documento para diferentes tipos de usuarios
 */
const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;
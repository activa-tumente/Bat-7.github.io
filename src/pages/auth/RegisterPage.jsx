import RegisterForm from '../../components/auth/RegisterForm';

/**
 * Página de registro que utiliza el componente RegisterForm
 * que soporta registro para diferentes tipos de usuarios
 */
const RegisterPage = () => {
  return <RegisterForm />;
};

export default RegisterPage;
